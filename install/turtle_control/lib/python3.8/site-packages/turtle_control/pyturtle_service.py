from pyturtle_interfaces.srv import MainBG

import rclpy
from rclpy.node import Node
from std_msgs.msg import String

class Pyturtle_Service(Node):

    def __init__(self):
<<<<<<< HEAD:install/turtle_control/lib/python3.8/site-packages/turtle_control/pyturtle_service.py
        super().__init__('pyturtle_service')
=======
        super().__init__('Pyturtle_Service')
>>>>>>> 2a9e282a9d545f3e8fe87822eca955d6e66c1803:src/turtle_control/turtle_control/bgcolor_service.py
        self.srv = self.create_service(MainBG, 'add_two_ints', self.add_two_ints_callback)

    def add_two_ints_callback(self, request, response):
        response.sum = request.a + request.b
        self.get_logger().info('Incoming request\na: %d b: %d c: %d' % (request.a, request.b, request.c))
        # Change_Color.Change()
        Change_Color(request.a, request.b,request.c )
        return response


class Change_Color(Node):        

    def __init__(self,a,b,c):
        super().__init__('change_color_publisher')
        self.publisher_ = self.create_publisher(String, 'topic', 10)
        timer_period = 0.5  # seconds
        msg = String()
        # print('{0} release'.format(key))
        msg.data = "change_color "+str(a)+" "+str(b)+" "+str(c)
        self.publisher_.publish(msg)


def main(args=None):
    rclpy.init(args=args)
    minimal_service = Pyturtle_Service()
    rclpy.spin(minimal_service)
    rclpy.shutdown()


if __name__ == '__main__':
    main()